apiVersion: v1
kind: Service
metadata:
  name: des
  labels:
    app: des
spec:
  selector:
    app: des
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: api
      port: 8000
      targetPort: 8000
  type: ClusterIP

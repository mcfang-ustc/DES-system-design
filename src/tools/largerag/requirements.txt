# LlamaIndex 核心
# Depend only on the concrete subpackages used by the project. Avoid the
# umbrella `llama-index` package because it pulls OpenAI integrations that
# conflict with the project's pinned DSPy/OpenAI stack and trigger resolver
# backtracking during Docker builds.
llama-index-core==0.14.20

# DashScope 集成
llama-index-embeddings-dashscope==0.5.0
llama-index-llms-dashscope==0.6.1
llama-index-postprocessor-dashscope-rerank==0.5.0

# 向量数据库
llama-index-vector-stores-chroma==0.5.5
chromadb==1.5.8

# 缓存系统（本地文件缓存为默认，无需额外依赖）
# Redis 缓存（可选）- 如需使用请取消注释：
# llama-index-storage-kvstore-redis>=0.2.0
# redis>=5.0.0

# 配置和工具
pydantic==2.11.7
python-dotenv==1.1.0
PyYAML==6.0.2

# 测试
pytest==8.4.2
pytest-asyncio==1.2.0
